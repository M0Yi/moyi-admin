**
!app/
!bin/
!config/
!composer.*
!public/
!storage/
!rephp.sh
!.env.example

# 不将本地环境变量文件打包进镜像，改为运行时注入
# （如果你确实需要某些非敏感配置，可改为专用配置文件）
# .env 文件将被忽略，因为我们以运行时环境变量或挂载方式注入brew install php@8.2

# 减少构建上下文，避免 vendor 随镜像上传；镜像内会执行 composer install
# 如需保留，请删除本行并在 Dockerfile 中跳过 composer 安装